<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dankerbell">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.samsung.android.health.permission.read"
            android:value="com.samsung.health.blood_pressure; com.samsung.health.blood_glucose" />

        <activity android:name=".LoginInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.SEARCH" />

            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <activity android:name=".homeActivity">
        </activity>
        <activity android:name=".ProfileActivity"/>
        <activity android:name=".myprofileActivity"/>
        <activity android:name=".bloodManagement.bloodActivity"></activity>
        <activity android:name=".mealManagement.mealActivity"></activity>
        <activity android:name=".mealManagement.SearchmealActivity"></activity>
        <activity android:name=".pillManagement.addpillActivity"></activity>
        <activity android:name=".pillManagement.pillActivity"></activity>
        <activity android:name=".Bluetooth.ConnectBluetoothActivity"></activity>
    </application>

</manifest>